# Introduction

If `created_at` and `revoked_at` are missing, i.e. `0`, in the database in 
vpn-user-portal they can be added using these tools from the actual 
certificates.

There is a slight difference of a couple of seconds, but close enough!

**NOTE**: these scripts are very brittle, use with care!
